# 卡牌配置表使用说明

本项目后续卡牌改动，以 [CARD_CONFIG_TABLE.csv](/d:/UsedToMakeMod/Mods/big-dog/CARD_CONFIG_TABLE.csv) 为唯一基准。

约定：

- 你后续直接改 `CSV`，我按这份表改代码、描述、本地化和相关效果。
- 如果代码当前实现与 `CSV` 冲突，一律以 `CSV` 为准。
- 表里没写的内容，默认保持当前实现不变。
- 同一张牌的 `描述`、`升级描述`、`基础效果`、`升级效果` 需要彼此一致。
- 如果只是改数值、不改描述，也请在表里明确写“描述不变”。

推荐编辑方式：

- 直接用 Excel 打开 `CARD_CONFIG_TABLE.csv`
- 或者用任意表格工具编辑后继续保存为 UTF-8 `CSV`

## 编码约定

- `CARD_CONFIG_TABLE.csv` 统一使用 `UTF-8 with BOM`
- 如果使用 Excel 保存，请优先选择 `CSV UTF-8 (逗号分隔)(*.csv)`
- 如果使用 WPS 或其他表格工具，保存时也要明确选择 `UTF-8`，不要使用 ANSI / 本地编码
- 不要用会自动按系统默认编码重写文件的脚本或编辑器直接覆盖这份表
- 如果终端里看到乱码，不代表文件一定损坏；先用 Excel 打开确认，再决定是否修复
- 如果文件里已经出现真正的 `�` 或 `锟斤拷`，那不是显示问题，而是内容已经被错误编码写坏，需要从最近正确版本重建

## 表驱动协作补充规则（2026-03-25）
- 以后实现卡牌时，默认只“读取” `CARD_CONFIG_TABLE.csv`，不主动改写表中的卡名、基础效果、升级效果、稀有度、费用、目标、标签/关键词等字段。
- 只有 `卡牌ID` 允许在缺失时由助手根据中文卡名生成稳定英文 ID；生成后再回写到表里。
- 用户只需要指出“哪些行更新了”，助手再去表中读取这些行并实现逻辑。
- 如果表与现有代码不一致：以表为准改代码，不反向改表。
- 卡牌描述和升级描述默认根据表中前面的效果列来同步补全本地化文本；若表里已有现成描述，则优先按表里描述落地。
- 若实现过程中发现表信息不足，先保持表原文不变，再单独向用户指出缺失项，不自行补写额外设计结论。
