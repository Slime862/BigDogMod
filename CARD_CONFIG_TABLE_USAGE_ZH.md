# 卡牌配置表使用说明

本项目后续卡牌改动，以 [CARD_CONFIG_TABLE.csv](/d:/UsedToMakeMod/Mods/big-dog/CARD_CONFIG_TABLE.csv) 为唯一基准。

约定：

- 你后续直接改 `CSV`，我按这份表改代码、描述、本地化和相关效果。
- 如果代码当前实现与 `CSV` 冲突，一律以 `CSV` 为准。
- 表里没写的内容，默认保持当前实现不变。
- 同一张牌的 `描述`、`升级描述`、`基础效果`、`升级效果` 需要彼此一致。
- 如果只是改数值、不改描述，也请在表里明确写“描述不变”。

推荐编辑方式：

- 直接用 Excel 打开 `CARD_CONFIG_TABLE.csv`
- 或者用任意表格工具编辑后继续保存为 UTF-8 `CSV`
